{
  "name": "immoscout-automation-functions",
  "description": "Cloud Functions backend for ImmoScout Automation licensing (Paddle Billing -> Firestore).",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@paddle/paddle-node-sdk": "^2.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
